name: db_viewer
description: The core implementation of all db_viewer packages
version: 1.1.0
homepage: https://github.com/vanlooverenkoen/db_viewer/db_viewer

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ">=6.0.0 <7.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
